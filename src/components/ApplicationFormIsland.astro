---
import ApplicationForm from "./ApplicationForm";

interface Props {
  variant?: "single" | "multi";
  prefill?: Record<string, any>;
}

const { variant = "multi", prefill } = Astro.props as Props;
---

<ApplicationForm client:load variant={variant} prefill={prefill} />
