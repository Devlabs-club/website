---
// Legacy /verify URL → new iMessage handoff page.
const t = Astro.url.searchParams.get('t') || '';
if (t) {
  return Astro.redirect(`/builder/start?t=${encodeURIComponent(t)}`);
}
return Astro.redirect('/builder/home');
---
